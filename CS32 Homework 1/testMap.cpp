//
//  testMap.cpp
//  cs32homework1
//
//  Created by Maeneka Grewal on 1/21/18.
//  Copyright © 2018 Maeneka Grewal. All rights reserved.
//

#include "Map.h"
#include <iostream>
#include <cassert>
using namespace std;

/*int main()
{
    Map m;  // maps strings to doubles
    assert(m.empty());
    ValueType v = -1234.5;
    assert( !m.get("abc", v)  &&  v == -1234.5); // v unchanged by get failure
    m.insert("xyz", 9876.5);
    assert(m.size() == 1);
    KeyType k = "hello";
    assert(m.get(0, k, v)  &&  k == "xyz"  &&  v == 9876.5);
    cout << "Passed all tests" << endl;
    
    Map a;
    Map b;
    a.insert("lmao", 55.3);
    a.insert("yolo", 99.32);
    a.insert("sma", 33234);
    a.insert("haha", 34);
    b.insert("YEET", 55.6);
    b.insert("COOL", 8823.3);
    cout << "This is a:" << endl;
    a.dump();
    cout << "This is b:" << endl;
    b.dump();
    a.erase("yolo");
    cout << "This is new a:" << endl;
    a.dump();
    a.swap(b);
    cout << "This is swapped a:" << endl;
    a.dump();
    cout << "This is swapped b:" << endl;
    b.dump();
    
    if (a.contains("YEET"))
    {
        cout << "contains is g so far" << endl;
    }
    if (!a.contains("idk man"))
    {
        cout << "contains is solid" << endl;
    }
    if (b.size() == 3)
        cout << "size is solid" << endl;
    b.update("sma", 23.0);
    b.dump();
    if (!b.update("ubby", 12))
        cout << "update is solid" << endl;
    a.insertOrUpdate("COOL", 13.0);
    a.insertOrUpdate("heyoo", 3.33);
    a.dump();
    b.erase("lmao"); b.erase("haha"); b.erase("sma");
    b.dump();
}*/
